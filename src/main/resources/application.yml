spring:
  datasource:
    control:
      url: jdbc:sqlite:control.db
      driver-class-name: org.sqlite.JDBC
  flyway:
    control:
      jdbcUrl: jdbc:sqlite:control.db
      driverClassName: org.sqlite.JDBC
app:
  version: v1